# services:
#   - type: web
#     name: osrm-backend
#     env: docker
#     plan: free
#     dockerfilePath: ./Dockerfile.osrm
#     autoDeploy: false
#     region: singapore
#     ports:
#       - port: 5000
#         protocol: http

services:
  - type: web
    name: osrm-backend
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: false
    region: singapore
    ports:
      - port: 10000   # Render public port, OSRM sẽ nghe $PORT bên trong container
        protocol: http
